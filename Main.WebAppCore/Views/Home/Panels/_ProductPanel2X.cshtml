@model PanelViewModel

<div class="container">

    <h4 class="sub-title text-justify">

        @Model.PanelTitle

    </h4>
  
    <div class="row">

        @{
            foreach ( var product in Model.ListPosts.ToList())
            {
                <div class="col-md-6 col-sm-6 col-lg-6 col-xs-6 card">
                           
                    <a class="thumbnail only-image">

                        @if ( product != null )
                        {

                            @await Html.PartialAsync( "~/Areas/PageContent/Views/Pages/Images/_Image2X.cshtml", product )
                                
                        }
                        else
                        {

                            @await Html.PartialAsync( "~/Areas/PageContent/Views/Pages/Images/_Image2X.cshtml", product)
                                    
                        }

                    </a>


                    <div class="caption card-body">

                        <h5 class="truncate text-bg-success card-title">

                            @(product?.PostTitle ?? "No Title Available")

                        </h5>

                        <p class="fa-dollar text-info-emphasis card-text">
                            @if ( product is not null )
                            {
                                <span>@(product.Price?.ToString("F2") ?? "0.00") Taka</span>
                            }
                            else
                            {
                                <span>Price unavailable</span>
                            }
                        </p>

                        <span>
                            <i class="fa fa-plus fa-fw"></i>

                            ADD TO CART
                        </span>

                    </div>
                
                </div>
            }
        }

    </div>

</div>